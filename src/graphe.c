#include "../include/graphe.h"
#include "../include/parsing.h"


//TreeNode *createNode(unsigned int id ,double x, double y){
//
//    TreeNode *node =(TreeNode *)malloc(sizeof(TreeNode)); ;
//    node->id = id ;
//    node->x = x ;
//    node->y = y ;
//    return node ;
//}

// create a graph from the given nodes (because it's all we need every tree is a neighbour to every other tree)



    //Graph *graph = malloc(sizeof(Graph));
//
    //graph->list_nodes = malloc(NBR_NODE*sizeof(TreeNode*));
//
    //for (int i = 0; i < NBR_NODE; i++)
    //    graph->list_nodes[i] = malloc( NBR_NODE * sizeof(TreeNode));
//
    //    
    //graph = createGraph(10000,tab_tree ,graph);
    //printf("%lf\n", (graph->list_nodes[99][109].x));
